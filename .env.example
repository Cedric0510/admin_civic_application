# Encore utilisé par les features pas encore migrées (rendez-vous, sondages,
# services, paramètres) — à retirer une fois la migration de civic_api terminée.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# civic_api — 4000 en local (voir civic_api/.env.example)
API_URL=http://localhost:4000
